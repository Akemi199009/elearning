<% if object.errors.any? %> 
  <div class="alert alert-danger"> 
      The form container <%= pluralize(object.errors.count, "error")%> <!-- errorの数をcountする -->
  </div>

  <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <%end%>
  </ul>
<hr >
<%end%>

